#include <iostream>
#include <cstring>

int main() {
    const char* s = "hello";
    std::cout << std::strlen(s) << "\n";

    // char buf[5];
    // std::strcpy(buf, "hello");
    // std::cout << buf << "\n";
}
